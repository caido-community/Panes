[tools]
node = "20"
pnpm = "9"

[tasks.validate]
description = "Run all quality checks (typecheck, lint, knip, test, build)"
run = [
  "pnpm typecheck",
  "pnpm lint",
  "pnpm knip",
  "pnpm test",
  "pnpm build",
]
